namespace RentIt

module main =
    [<EntryPoint>]
    let main argv = 
      (*
      let actionGroup = "action group"
      let userId = "1"
      let userType = "admin"
      let permission = "edit"
      let testPermission = "delete stuff"
      let testActionGroup = "action group 2"
      let testUserType = "test user type"
      let mutable result = ""
      // Check permission
      printfn "%A" ("userId: " + userId + " permission: " + permission + (string (Permissions.CheckUserPermission userId permission)))

      // Assign permission
      let result = Permissions.AssignPermissionToActionGroup actionGroup permission
      let result = "Assign permission " + actionGroup + " " + permission + " " + (string (result))
      printfn "%A" result;

      // Delete permision
      let result = Permissions.UnassignPermissionFromActionGroup actionGroup permission
      let result = "Delete permission " + actionGroup + " " + permission + " " + (string (result))
      printfn "%A" result;

      // Add Permision
      let result = Permissions.AddAllowedAction testPermission "yup"
      let result = "Add permission " + testPermission + " " + (string (result))
      printfn "%A" result;

      // Add Action Group
      let result = Permissions.AddActionGroup testActionGroup "test"
      let result = "Add Action Group " + testActionGroup + " " + (string (result))
      printfn "%A" result;

      // Delete Action Group
      let result = Permissions.DeleteActionGroup actionGroup
      let result = "Delete Action Group " + actionGroup + " " + (string (result))
      printfn "%A" result;

      // Assign Action Group to User Type
      let result = Permissions.AssignActionGroupToUserType userType actionGroup
      let result = "Assign Action Group to User Type " + userType + " " + actionGroup + " " + (string (result))
      printfn "%A" result;

      // Unassign Action Group to User Type
      let result = Permissions.UnassignActionGroupToUserType userType actionGroup
      let result = "Unassign Action Group to User Type " + userType + " " + actionGroup + " " + (string (result))
      printfn "%A" result;

      // Add User Type
      let result = Permissions.AddUserType testUserType
      let result = "Add User Type " + testUserType + " " + (string (result))
      printfn "%A" result;

      // Delete User Type
      let result = Permissions.DeleteUserType userType
      let result = "Delete User Type " + userType + " " + (string (result))
      printfn "%A" result;
      *)
      0